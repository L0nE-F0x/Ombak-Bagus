# Installers

| File | Platform |
|------|----------|
| `Ombak-Bagus-Setup-*.exe` | Windows (native) |
| `Ombak-Bagus.apk` | Android (sideload) - produced by **Android APK** GitHub Action |

Mac installers live on GitHub Releases. iOS uses the `/app/` PWA (no IPA).
